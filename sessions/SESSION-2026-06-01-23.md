# SESSION-2026-06-01-23

- **Роль:** архитектор (декомпозиция M4)
- **Начало/конец (UTC):** 2026-06-01 — / 2026-06-01
- **Задачи в фокусе:** постановка задач M4 (уведомления)
- **Майлстоун:** M4

## Цель сессии
M3 принят (AUDIT-M3). Разбить M4 (Уведомления) на одно-сессионные задачи по ADR-0005.

## Что сделано
- Сверил контекст: `scheduler/app.py` и `services/reminders.py` — заглушки; таблица
  `sent_reminders` уже в схеме (миграция не нужна); `ChargeRepository.mark_paid` и FSM
  редактирования готовы (M3).
- Поставил три задачи M4 (по слоям: логика/данные → приём → свип):
  - **TASK-0014** (high) — чистая due-логика напоминаний (эффективные дни global/individual/off,
    срабатывание за N дней, snooze-повтор, выбор пользователей по `notify_time`/`tz`) +
    `SentReminderRepository` (идемпотентность) + `ChargeRepository.snooze`. Без планировщика/Telegram.
  - **TASK-0015** (high) — текст уведомления + кнопки «Оплачено/Отложить/Редактировать» (FR-12),
    специфичные `remind_*` + router-тест. Зависит от 0014.
  - **TASK-0016** (high) — свип-планировщик APScheduler (NFR-1/NFR-2): тик раз в минуту (UTC),
    выбор по БД, отправка, запись `sent_reminders`, рестарт-безопасность/идемпотентность. Зависит от 0015.

## Принятые решения
- Архитектура уведомлений строго по ADR-0005 (свип + sent_reminders, не джоба-на-списание).
- «Отложить» = `snoozed_until = завтра`, `next_date` неизменна.
- В каждую задачу — обязательный полный прогон CI, router-тест для callback'ов, идемпотентность.

## Изменения состояния
- `state/project.json`: `last_session` → SESSION-2026-06-01-23; `next_step` — исполнитель берёт TASK-0014.
- handoff: в `inbox/` — TASK-0013 (low, router-тест charges) и M4: TASK-0014/0015/0016.

## Что осталось / следующий шаг
1. Исполнитель: `/take-task` → **TASK-0014** → 0015 → 0016. (TASK-0013 — low, в любой момент.)
2. После M4 — **строгий аудит M4** (рестарт-безопасность, отсутствие задвоений, точность ±1 мин).
3. Далее M5 (стабилизация и деплой 24/7).

## Коммиты
- `<hash>` chore(M4): постановка TASK-0014/0015/0016 (уведомления)
