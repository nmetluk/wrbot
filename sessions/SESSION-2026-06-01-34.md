# SESSION-2026-06-01-34

- **Роль:** исполнитель
- **Начало/конец (UTC):** 2026-06-02 ~00:20 / TBD
- **Задачи в фокусе:** TASK-0020 (детерминированный CI — honoring uv.lock)
- **Майлстоун:** M5

## Цель сессии
Взять и реализовать TASK-0020: устранить нондетерминизм CI по версиям ruff/mypy/pytest и т.д. Перевести `.github/workflows/ci.yml` (и локальные инструкции) на `uv sync --frozen --extra dev` + `uv run <tool>`, чтобы локальный и CI-прогоны использовали ровно те же pinned-версии из `uv.lock`.

## Что сделано
- Ритуал: `git pull --ff-only`, прочтение AGENTS.md / state/project.json / SESSION-2026-06-01-33.
- `/take-task` (через scripts/take_task.py): TASK-0020 перемещена `inbox/` → `in-progress/`, backlog обновлён (in_progress=1).
- Создана сессия SESSION-2026-06-01-34.
- Обновлён `state/project.json`: active_task=TASK-0020, last_session=SESSION-2026-06-01-34, current_phase уточнена.
- Изменён `.github/workflows/ci.yml`:
  - Удалены `actions/setup-python` + pip cache + `pip install -e ".[dev]"`.
  - Добавлен `astral-sh/setup-uv@v3`, `uv python install 3.11`, `uv sync --frozen --extra dev`.
  - Все гейт-команды переведены на `uv run ...` (ruff, mypy, pytest, alembic, validate, scripts lint).
- Обновлены `docs/workflow/executor-guide.md` и `CONTRIBUTING.md`: явно указано прогонять гейт через `uv run` (как в CI), с примером команд.
- Обновлён `state/CHANGELOG.md` (Unreleased / In progress).

## Принятые решения
- Использован `astral-sh/setup-uv@v3` + `uv python install 3.11` + `uv sync --frozen --extra dev` (стандартный, воспроизводимый способ для uv-based проектов в 2026).
- Python в CI: 3.11 (как раньше), инструменты — строго из lock (ruff==0.15.15, mypy==2.1.0, pytest==9.0.3).
- Alembic-шаг: `BOT_TOKEN=test uv run alembic upgrade head` (env var префиксом, без отдельного pip install -e .).
- Pre-commit оставлен без изменений (его ruff rev v0.8.0 — отдельный хук, не влияет на CI gate).
- Не добавлял `.python-version` (пока не требуется; uv резолвит из pyproject + lock).
- README install section не трогал (минимальный scope по критериям приёмки; uv упомянут в CONTRIBUTING/executor-guide).

## Изменения состояния
- `state/project.json`: active_task → TASK-0020, last_session → SESSION-2026-06-01-34, current_phase → "M5: ... (в работе)", next_step обновлён, updated_at.
- handoff: TASK-0020: inbox/ → in-progress/ (скриптом take_task).
- `state/backlog.json`: обновлён (inbox=5, in_progress=1).
- `state/CHANGELOG.md`: добавлена запись о старте TASK-0020.
- `.github/workflows/ci.yml`, `docs/workflow/executor-guide.md`, `CONTRIBUTING.md` изменены.

## Что сделано (дополнительно / verification)
- После редактирования: полный прогон CI-команд на **чистом** окружении (удалил `.venv`, `uv python install 3.11`, `uv sync --frozen --extra dev`).
  - ruff format/check, mypy, pytest (145 passed), alembic, validate, scripts lint — **все 0**.
  - Инструменты: ruff 0.15.15, mypy 2.1.0, pytest 9.0.3 (точно из lock).
  - Python: 3.11.15 (точно как в CI).
- Заполнен отчёт `handoff/reports/TASK-0020-report.md`.
- Задача завершена: `complete_task.py` → done/, backlog обновлён (done=20).
- Обновлён `state/project.json` (active_task=null, last_session=34, next_step указывает на 0021).
- Дописан `state/CHANGELOG.md` (Unreleased: In progress → завершение TASK-0020).
- Сессия полностью задокументирована.

## Что осталось / следующий шаг
- `/end-session` завершён: отчёт, state, CHANGELOG, лог, commit+push.
- **Следующий шаг для исполнителя:** `/take-task` → TASK-0021-m5-resilience-errors.md (high prio, M5).
- После 0020-0024 — отдельная сессия финального аудита M5.

## Коммиты
- `ci(TASK-0020): deterministic toolchain via uv sync --frozen + uv run (Task: TASK-0020)`
  Файлы: .github/workflows/ci.yml, docs/workflow/executor-guide.md, CONTRIBUTING.md, state/*, handoff/*, sessions/SESSION-2026-06-01-34.md, reports/TASK-0020-report.md


