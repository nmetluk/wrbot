# SESSION-2026-06-04-14

- **Роль:** архитектор-аудитор (аудит M8 — валюты)
- **Дата (UTC):** 2026-06-04
- **Майлстоун:** M8

## Вердикт: ПРИНЯТО (отчёт AUDIT-M8-2026-06-04.md)
Обе задачи (0049, 0050) в `done/`, гейт начисто зелёный.

## Проверено независимо
- Гейт: ruff/mypy (46)/**pytest 263**/validate; миграция `alembic check` пусто, round-trip ок;
  `charges.currency` server_default+backfill, `users.last_currency` nullable (дефолт RUB в коде).
- Загрузчик `currencies.py` (оффлайн): format_amount RUB→`₽`/USD→`$`/не-пресеты→код; search/is_valid ок; 153 валюты.
- Зашитый «₽» убран (grep чисто).
- Выбор валюты: пресеты + «Другая» (список+поиск), last_currency, edit. Роутинг без шадовинга
  (стейт-фильтр + различимые префиксы; широкого перехватчика нет). Router+e2e тесты (+22).

## Следующий шаг
- Релиз **v0.5.0** (bump 0.5.0, /CHANGELOG.md [0.5.0], release-notes, тег) — по решению владельца.
- Вне кода: деплой (миграция currency только после бекапа) + живой смоук (создать списание в др. валюте).
