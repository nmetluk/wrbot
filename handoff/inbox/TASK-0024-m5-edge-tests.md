---
id: TASK-0024
title: "M5: edge/устойчивость-тесты и финальная полировка перед аудитом"
milestone: M5
status: inbox
priority: medium
created_by: architect
created_at: 2026-06-01T23:00:00Z
depends_on: [TASK-0021]
---

# TASK-0024: Edge-тесты и финальная полировка

## Цель
Покрыть граничные сценарии и подготовить проект к финальному аудиту перед релизом (NFR-1/NFR-3).

## Контекст
- ТЗ §5. Готовы M1–M4 + устойчивость (TASK-0021), деплой (TASK-0022).

## Критерии приёмки (проверяемые)
- [ ] **Edge-сценарии покрыты тестами:** создание списания без кошельков (предложение создать);
      «Мои списания» при пустом списке; много списаний (сортировка/постранично или предел);
      повторная «Оплата» одного списания (нет двойного сдвига); tz-границы (полночь, смена суток);
      рестарт во время «дня уведомления» (свип не задваивает); удаление кошелька с активными списаниями (из 0021).
- [ ] **End-to-end smoke** (по возможности без реального Telegram): старт → миграция → создание
      пользователя/кошелька/категории/списания → due-расчёт → отправка (mock bot) → mark_paid/snooze.
- [ ] Проверены открытые вопросы `requirements.md`: каскад удаления кошелька (закрыт в 0021),
      лимиты (M2) — отметить статус; зафиксировать оставшиеся (напр. UI смены TZ) как v2.
- [ ] Покрытие тестами ключевой логи (даты, due, mark_paid, свип) — без регрессий.
- [ ] Весь CI начисто зелёный.

## Ожидаемые артефакты
- Тесты: `tests/test_edge_*.py`, `tests/test_e2e_smoke.py`.
- При необходимости — мелкие правки кода (с отдельной отметкой).

## Ограничения / заметки
- Не расширять функциональность (это v2). Цель — устойчивость и подтверждение готовности к релизу.
- После этой задачи — **финальный комплексный аудит** (отдельная сессия). Перед `done`: полный CI, отчёт, лог, push.

## Зависимости
Зависит от: TASK-0021
