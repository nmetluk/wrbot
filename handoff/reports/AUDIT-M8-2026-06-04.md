# AUDIT-M8 (валюты) — 2026-06-04

- **Аудитор/сессия:** SESSION-2026-06-04-14 (архитектор-аудитор)
- **Майлстоун:** M8 — валюты списаний (TASK-0049, TASK-0050; ADR-0013)
- **Вердикт:** **ПРИНЯТО.** Обе задачи в `done/`, гейт начисто зелёный. Готов к релизу v0.5.0.

## Гейт начисто (версии uv.lock; shim UTC, MPLBACKEND=Agg)
- ruff format/check ✅, mypy ✅ (46 файлов, strict), **pytest 263** ✅, validate ✅.
- Миграция `currency_fields_task_0049`: `alembic upgrade head` ✅, `alembic check` пусто (модели ≡ схема),
  downgrade −1 → upgrade round-trip ✅.

## TASK-0049 (схема + справочник + отображение) — принято
- `charges.currency` (`String(3)`, NOT NULL, **server_default='RUB' + явный backfill** — безопасно на
  непустой БД); `users.last_currency` (`String(3)`, nullable, дефолт RUB на уровне кода). Downgrade корректен.
- `services/currencies.py` — оффлайн-загрузчик `currencies.json` (модульный кэш): `get_presets`/
  `get_default`/`get`/`is_valid_code`/`search`/`format_amount`. Проверено независимо:
  `format_amount` 1000 RUB→`1000 ₽`, USD→`1000 $`, GBP/AMD (не-пресеты)→код; `search("pound")`→
  EGP/FKP/GBP/…; `is_valid` GBP=True/ZZZ=False; 153 валюты.
- Зашитый «₽» убран из `texts.py`/`charges_list.py` (grep чисто; остался только символ в данных
  `currencies.json` и комментарий). Суммы рендерятся через форматтер по `charge.currency`.

## TASK-0050 (выбор валюты) — принято
- Шаг `NewChargeStates.currency` (+ `currency_search`) после суммы; клавиатура: 5 пресетов
  (преселект = `user.last_currency`) + «🌐 Другая валюта».
- «Другая» → постраничный список (`charge_currency_page_*`) + ввод кода/названия → `search` →
  результаты кнопками (`charge_currency_choose_*`); «◀️ К пресетам» (`back`).
- **Роутинг без шадовинга (урок TASK-0046):** все валютные хэндлеры со стейт-фильтром
  `NewChargeStates.currency`, префиксы различимы (`preset_`/`page_`/`choose_`/exact `other`/`back`),
  широкого `charge_currency_` перехватчика нет. Router-level + e2e тесты добавлены (+22 теста, 263 всего).
- `User.last_currency` обновляется при создании и преселектит в следующий раз; смена валюты в
  редактировании поддержана; сводка/карточка/список показывают валюту.

## Замечания
- Нет (для не-пресетных валют показывается код вместо символа — осознанно по ADR-0013, однозначно).

## Итог
**M8 ПРИНЯТ.** Срочный отзыв по валютам закрыт: валюта у каждого списания, оффлайн-справочник ISO 4217,
пресеты + список/поиск, запоминание последней. Следующий шаг — релиз **v0.5.0**.
