# AUDIT-M2 (повторный, r2) — 2026-06-01

- **Аудитор/сессия:** SESSION-2026-06-01-16 (архитектор, независимо от исполнителя)
- **Майлстоун:** M2 — Справочники (кошельки и категории)
- **Вердикт:** **ПРИНЯТО**
- **Предыдущий аудит:** `AUDIT-M2-2026-06-01.md` — НЕ ПРИНЯТО (mypy/ruff красные).

## Охват
Проверены TASK-0004 (слой данных), TASK-0005 (бот-UI), а также исправления
TASK-0006 (mypy), TASK-0007 (ruff), TASK-0008 (маршрутизация callback'ов).

## Автоматические проверки (чистый venv, БЕЗ BOT_TOKEN, как CI)
- `ruff format --check src tests`: ✅ (после механического `ruff format`
  `tests/test_handlers_settings.py` — см. ниже)
- `ruff check src tests`: ✅
- `ruff check scripts`: ✅
- `mypy src`: ✅ Success (0 ошибок, --strict)
- `pytest`: ✅ **96 passed** (включая `tests/test_callback_routing.py`)
- `alembic upgrade head`: ✅ (без BOT_TOKEN)

## По направлениям
| Направление | Оценка | Комментарий |
|-------------|--------|-------------|
| Безопасность | ✅ | Изоляция по `tg_id` (FR-13) во всех репозиториях; секреты вне репо; валидация ввода |
| Архитектура | ✅ | Слои Handlers→Repositories→Models; DI через `DbSessionMiddleware`; ADR-0003 соблюдён |
| Качество кода | ✅ | mypy --strict 0, ruff 0; маршрутизация callback'ов исправлена и **покрыта тестом через роутер** |
| Соответствие требованиям | ✅ | FR-8 (кошельки CRUD), FR-9 (категории CRUD) реализованы |
| Надёжность 24/7 | ⏸ | Не применимо к M2 (проверка в M4/M5) |
| Процесс/контекст | ✅ | handoff/state согласованы; отчёты и логи на месте |

## Проверка ключевого фикса (TASK-0008)
- Широкие `F.data.startswith("wallet_"/"category_")` удалены; item-кнопки →
  `wallet_item_<id>`/`category_item_<id>`; details-фильтры на `*_item_`.
- `tests/test_callback_routing.py` инспектирует реально зарегистрированные на роутере
  фильтры (то, что использует Dispatcher), а не прямой вызов — корректный подход.

## Замечания (не блокирующие)
1. **Формат-долг.** CI-шаг `ruff format --check src tests` падал на
   `tests/test_handlers_settings.py` (исполнитель оставил «пре-экзистентный долг»).
   Применён механический `ruff format` к этому файлу (без изменения логики), гейт стал
   зелёным. Также вне CI-проверки остаётся формат-долг в `scripts/` (3 файла). → **TASK-0009**
   (pre-commit + чистка) чтобы дрейф не повторялся.
2. **`# type: ignore` в хэндлерах (TASK-0006).** Локализованы на гарантированных F.data-парсингах;
   mypy зелёный. Допустимо, но при рефакторинге M3 желательно заменить на явное сужение.

## Итог
**M2 ПРИНЯТ.** Критичные находки прошлого аудита (mypy, ruff) и функциональный баг
маршрутизации устранены, CI честно зелёный из чистого окружения. Заведена нестрогая
**TASK-0009** (pre-commit/формат-долг). Следующий шаг — старт **M3** (списания).
