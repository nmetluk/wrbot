# AUDIT-M5 / Финальный аудит перед релизом — 2026-06-02

- **Аудитор/сессия:** SESSION-2026-06-02-01 (архитектор-аудитор)
- **Охват:** весь проект (M0–M5), готовность к релизу v1
- **Вердикт:** **НЕ ПРИНЯТО к релизу** — 1 блокер соответствия ТЗ (FR-10 не реализован).
  Всё остальное (CI, безопасность, надёжность, деплой) — готово.

## Автоматические проверки (venv с версиями из uv.lock: ruff 0.15.15 / mypy 2.1.0)
- `ruff format --check src tests`: ✅
- `ruff check src tests` / `ruff check scripts`: ✅
- `mypy src`: ✅ Success (35 файлов, --strict)
- `pytest`: ✅ **157 passed**
- `alembic upgrade head`: ✅
- `scripts/validate.py`: ✅
- CI теперь детерминирован: `uv sync --frozen --extra dev` + `uv run` (TASK-0020).

## Блокер (соответствие ТЗ)
- **FR-10 (Глобальные уведомления) НЕ реализован.** Кнопка «🔔 Глобальные уведомления»
  (`settings_global`) — заглушка `global_settings_stub`; `SettingsStates` закомментирован;
  смены `notify_time` и `global_days` в UI нет. Пользователь **не может изменить время
  уведомлений (остаётся 10:00) и глобальные дни (5/3/1)** — а ТЗ §3.3 требует это редактировать.
  Причина: при декомпозиции M4 заложен движок + per-charge (FR-11), но UI глобальных настроек упущен.
  → **TASK-0026** (high, блокер релиза).

## По направлениям
| Направление | Оценка | Комментарий |
|-------------|--------|-------------|
| Безопасность | ✅ | Секреты в `.env` (gitignore + dockerignore); образ без секретов; изоляция по `tg_id` во всех репозиториях; валидация ввода |
| Архитектура | ✅ | Слои Handlers→Services→Repositories→Models; ADR-0003…0007 соблюдены; миграции только Alembic |
| Качество кода | ✅ | mypy 0, ruff 0, 157 тестов; pre-commit; детерминированный CI |
| Надёжность 24/7 | ✅ | Глобальный error-handler (без re-raise), graceful shutdown по SIGINT/SIGTERM, свип идемпотентен и рестарт-безопасен (NFR-1), отправка в `notify_time` (NFR-2) |
| Деплой | ✅ | Dockerfile (multi-stage, uv+lock, non-root), entrypoint с миграциями, compose (restart, volume, профиль postgres), `docs/deployment.md` |
| Соответствие требованиям | ⚠ | FR-1…9, FR-11…14 — реализованы; **FR-10 — нет** (блокер). NFR-1…7 — ок |
| Процесс/контекст | ✅ | handoff/state/sessions/ADR/CHANGELOG целостны |

## Трассировка FR (кратко)
FR-1 ✅ меню, FR-2 ✅ /cancel /help, FR-3 ✅ создание, FR-4 ✅ периоды, FR-5 ✅ список,
FR-6 ✅ действия, FR-7 ✅ оплачено/сдвиг, FR-8 ✅ кошельки, FR-9 ✅ категории,
**FR-10 ❌ глобальные уведомления (время+дни)**, FR-11 ✅ индивидуальные дни,
FR-12 ✅ уведомление с кнопками, FR-13 ✅ изоляция, FR-14 ✅ русский.
Бонус (ADR-0004, по решению владельца): выбор часового пояса (TASK-0025).

## Замечания (не блокирующие)
1. **(minor)** Healthcheck в compose — тривиальный (всегда «healthy», не проверяет живость бота).
   Для прод-наблюдаемости позже стоит добавить реальный сигнал. → можно в v2/эксплуатацию.
2. **(info)** Часть открытых вопросов requirements закрыта (лимиты, каскад, помощь, tz).

## Итог
Проект технически зрелый и готов к эксплуатации **после закрытия FR-10**. Рекомендация:
закрыть **TASK-0026** (UI глобальных уведомлений), затем повторный быстрый аудит соответствия
ТЗ → релиз v1. Доработки §6 ТЗ — отдельная итерация v2.
