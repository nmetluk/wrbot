# AUDIT-M4 — 2026-06-01

- **Аудитор/сессия:** SESSION-2026-06-01-28 (архитектор-аудитор, независимо)
- **Майлстоун:** M4 — Уведомления (свип, дни, отправка с кнопками, «Отложить»)
- **Вердикт:** **НЕ ПРИНЯТО** (красный CI: mypy + функциональный блокер: время уведомлений)

## Охват
TASK-0014 (due-логика + sent_reminders + snooze), TASK-0015 (текст+кнопки),
TASK-0016 (свип-планировщик), TASK-0013 (router-тест charges).

## Автоматические проверки (чистый venv, как CI; shim UTC для 3.10)
- `ruff format --check .`: ✅
- `ruff check .`: ✅
- `mypy src`: ❌ **12 ошибок** в `src/wrbot/bot/handlers/reminders.py` (union-attr)
- `pytest`: ✅ 144 passed
- `alembic upgrade head`: ✅

## Находки
| # | Серьёзность | Описание | Задача |
|---|-------------|----------|--------|
| 1 | **BLOCKER (функц.)** | Свип НЕ учитывает `notify_time`/`tz`. `run_sweep`→`get_due_reminders_today` выбирает due только по дате; `select_users_to_notify_at` (есть в TASK-0014) нигде в свипе не вызывается. → Напоминание уходит на первом тике суток (≈полночь UTC), а не в выбранное время (например 10:00 МСК). Нарушает ТЗ §3.4 и NFR-2. `test_sweep` время не проверяет. | TASK-0017 |
| 2 | **BLOCKER (CI)** | `mypy src` — 12 ошибок в `handlers/reminders.py` (`callback.data.split` на `str|None`; `callback.message.edit_text` на `Message|InaccessibleMessage|None`). Отчёты 0015/0016 гоняли mypy по своим файлам, не `mypy src`. | TASK-0018 |
| 3 | major (переносимость) | `SentReminderRepository.record` использует SQLite-only `INSERT OR IGNORE` (`dialects.sqlite ... prefix_with("OR IGNORE")`) → на PostgreSQL невалидный SQL. Противоречит ADR-0003 (путь SQLite→PostgreSQL). Для v1 (SQLite) работает. | TASK-0019 |

## По направлениям
| Направление | Оценка | Комментарий |
|-------------|--------|-------------|
| Безопасность | ✅ | Изоляция tg_id; секреты вне репо |
| Архитектура | ⚠ | Слои и ADR-0005 в целом соблюдены, НО ключевой helper выбора по времени не подключён (находка 1); запись не переносима (находка 3) |
| Качество кода | ❌ | mypy красный (находка 2) |
| Соответствие требованиям | ❌ | FR-12 (кнопки) есть, но «в выбранное время» (§3.4)/NFR-2 нарушены (находка 1) |
| Надёжность 24/7 | ⚠ | Идемпотентность/рестарт через `sent_reminders` — ок (тест 2 тика=1 отправка); но без тайминга это «надёжно шлём в неверное время» |
| Процесс/контекст | ✅ | handoff/state согласованы; отчёты/логи на месте; TASK-0013 (router-тест charges) закрыт корректно |

## Что проверено положительно
- Due-логика (`get_effective_days`, `get_due_target_days`) и snooze — корректны.
- `select_users_to_notify_at` сам по себе верен (астимзона, минутная точность) — но **не используется**.
- Идемпотентность `was_sent`/`record` (на SQLite), запись только после успешной отправки, изоляция ошибок в свипе.
- `test_callback_routing.py` теперь покрывает и `charge_*` (TASK-0013).

## Итог
**M4 НЕ ПРИНЯТ.** Два блокера: (1) уведомления уходят не в `notify_time`, (2) mypy красный.
Заведены TASK-0017 (тайминг свипа, BLOCKER), TASK-0018 (mypy reminders, BLOCKER),
TASK-0019 (переносимость record на PostgreSQL, major). После 0017+0018 (зелёный гейт +
тест тайминга) — повторный аудит M4.
