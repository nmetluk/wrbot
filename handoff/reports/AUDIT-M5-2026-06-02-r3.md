# AUDIT-M5 / Релиз-аудит (r3, финальный) — 2026-06-02

- **Аудитор/сессия:** SESSION-2026-06-02-04 (архитектор-аудитор)
- **Охват:** проверка TASK-0027 (критический фикс) + готовность v1 к релизу
- **Вердикт:** **ПРИНЯТО — v1 готов к релизу.**

## Гейт (venv с версиями из uv.lock; как CI)
- `ruff format --check` / `ruff check` (src/tests) ✅
- `mypy src` ✅ (35 файлов, --strict)
- `pytest` ✅ **179 passed** (вкл. `tests/test_fsm_session_lifecycle.py` — 5 интеграционных)
- `alembic upgrade head` ✅; `validate.py` ✅; CI детерминирован (uv.lock).

## Закрытие критической находки r2 (TASK-0027)
- Сессия БД больше **не хранится в FSM** (`state.update_data(session=...)` и
  `state_data.get("session")` удалены во всех хэндлерах). Continuation-хэндлеры принимают
  `session: AsyncSession` из `DbSessionMiddleware` текущего апдейта.
- **Независимое репро (реальная SQLite):** смена `notify_time` 10:00→09:00 теперь **сохраняется**
  (в БД 09:00). Баг тихой потери данных устранён.
- Добавлен интеграционный тест жизненного цикла сессии (реальная фабрика + middleware, две
  итерации апдейтов) — покрывает FSM-записи; закрывает пробел покрытия, тянувшийся с M2.

## Итоговая трассировка ТЗ
- **FR-1…FR-14** — реализованы и фактически персистятся (проверено).
  FR-10 (глобальные уведомления: время+дни) — TASK-0026; FR-11/FR-12 — M4.
- **NFR-1** (24/7: error-handler, graceful shutdown, идемпотентный рестарт-безопасный свип) ✅,
  **NFR-2** (отправка в notify_time, минутная точность) ✅, **NFR-3** (mypy/ruff/тесты/доки) ✅,
  **NFR-4** (слои/расширяемость) ✅, **NFR-5** (стек) ✅, **NFR-6** (секреты вне репо/образа) ✅,
  **NFR-7** (кроссплатформенность, uv/lock) ✅.
- Бонус: выбор часового пояса (TASK-0025).

## Замечания (не блокирующие, для эксплуатации/v2)
1. Healthcheck в docker-compose тривиальный (всегда «healthy») — добавить реальный сигнал в эксплуатации.
2. §6 ТЗ (экспорт CSV/PDF, статистика, мультивалюта, семейный режим) — отдельная итерация v2.

## Итог
**v1 ПРИНЯТ к релизу.** Все FR/NFR выполнены, гейт честно зелёный и детерминирован,
критические дефекты устранены и покрыты тестами. Рекомендация: проставить релиз-тег
(версия из `pyproject.toml`) и обновить корневой `CHANGELOG.md` записью релиза. Далее — план v2.
